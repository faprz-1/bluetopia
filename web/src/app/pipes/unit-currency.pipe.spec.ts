import { UnitCurrencyPipe } from './unit-currency.pipe';

describe('UnitCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
